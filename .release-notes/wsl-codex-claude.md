# 支持 WSL 中的 Codex 和 Claude Code 会话

## 新增功能

- Windows 用户现在可以添加 WSL 发行版，搜索、查看并继续其中的 Codex 和 Claude Code 会话。

## Bug 修复

- WSL 会话恢复失败时会显示准确的 WSL 连接错误提示。
- WSL 环境名称现在只显示一次 WSL 前缀，例如 `WSL · Ubuntu`。
- 删除 WSL 会话后，已删除的会话不会在下一次同步时重新出现。
